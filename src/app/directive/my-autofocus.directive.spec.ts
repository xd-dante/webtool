import { MyAutofocusDirective } from './my-autofocus.directive';

describe('MyAutofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAutofocusDirective();
    expect(directive).toBeTruthy();
  });
});
